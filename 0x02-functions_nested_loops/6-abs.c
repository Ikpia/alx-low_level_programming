#include "main.h"

/**
* _abs - function that computes the absolute value of an integer
* @n: vairable is an int
* Return: an int
*/

	int _abs(int n)
{
	if (n >= 0)
{
	return (n);
}

	else
{
	return (-1 * n);
}
}
